{
  "name": "@anthropic/antigravity-persistence",
  "version": "0.1.0",
  "description": "Antigravity Workflow Runtime \u2014 \u6301\u4e45\u5316\u5c42 (JSONL EventStore + SQLite CheckpointStore + Memory)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "npm run clean && tsc -b --pretty"
  },
  "dependencies": {
    "@anthropic/antigravity-shared": "*",
    "@anthropic/antigravity-core": "*",
    "better-sqlite3": "^11.0.0",
    "minisearch": "^7.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.3.0"
  }
}
