{
  "name": "@anthropic/conductor-core",
  "version": "0.1.0",
  "description": "Conductor AGC — DAG 引擎、风险路由、合规引擎、应用服务",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@anthropic/conductor-shared": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
