import React from 'react';
/**
 * iOS / SF Symbols style inline SVG icons (Lucide-based).
 * Shared across all Conductor Hub panels.
 */
declare const Icon: React.FC<{
    name: string;
    size?: number;
    color?: string;
}>;
export default Icon;
