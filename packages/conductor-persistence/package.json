{
  "name": "@anthropic/conductor-persistence",
  "version": "0.1.0",
  "description": "Conductor AGC — 持久化层 (JSONL EventStore + SQLite CheckpointStore + Memory)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@anthropic/conductor-shared": "*",
    "@anthropic/conductor-core": "*",
    "better-sqlite3": "^11.0.0",
    "minisearch": "^7.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.3.0"
  }
}
