{
  "name": "@anthropic/antigravity-core",
  "version": "1.0.0",
  "description": "Antigravity Workflow Runtime — DAG 引擎、风险路由、合规引擎、应用服务",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "npm run clean && tsc -b --pretty",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@anthropic/antigravity-shared": "*",
    "@anthropic/antigravity-model-shared": "*",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
